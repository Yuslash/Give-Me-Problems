const screenWidth = window.innerWidth
const screenHeight = window.innerHeight

console.log(`Width: ${screenWidth}, Height: ${screenHeight}`)
// how ar you da venna that is not the vscode that is something else 